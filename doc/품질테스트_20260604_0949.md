# Shimmaru(쉼마루) 전체 통합 품질 검사 보고서

- **검사일시:** 2026-06-04 09:49
- **검사자:** 품질담당자 (심소현)
- **검사범위:** 정적분석 · 빌드 · 런타임 · 빅데이터 API 통합 · 다국어 · 라우팅 · 포트/실행환경
- **기준 보고서:** 품질테스트_20260602_1537.md 의 지적사항 후속 검증

---

## ✅ 통과 항목

| 검사 | 결과 |
|---|---|
| TypeScript 컴파일 (`tsc -b`) | ✅ 에러 0 |
| ESLint (`npm run lint`) | ✅ **exit 0** (이전 20 errors → 0, 경고 5건만 잔존·비차단) |
| 다국어 키 정합성 (ko/en/ja/zh) | ✅ bigdata 네임스페이스 포함 4개 언어 일치 |
| **빅데이터 — 데이터랩 방문자수(DataLabService)** | ✅ **활용신청 승인·실데이터 렌더 확인** (아래 #1) |
| 관광정보 TourAPI (KorService2) | ✅ resultCode 0000, 경북 데이터 수신 |
| 라우팅 (`/insights` 포함) | ✅ 전 화면 헤드리스 렌더 정상, 런타임 에러 0 |
| **포트 5173 고정 점유** | ✅ **predev free-port 스크립트로 자동 확보**(아래 #4) |
| 핵심 화면 런타임(헤드리스) | ✅ 홈·축제·탐색·장소상세·인사이트 정상 |

---

## 🟢 이전 보고서 지적 → 해소된 항목

### #1(이전 🔴) 빅데이터 "활용신청 안내" 죽은 코드 → **수정 완료 + 데이터 실연동**
- `bigdata.ts callBigData` 에 **`validateStatus: () => true`** 적용 — 4xx/5xx 평문/JSON 응답을 더 이상 axios 가 throw 하지 않고 상태코드·본문으로 `not-subscribed`/`error`/`ok` 분류.
- 이후 **`한국관광공사_관광빅데이터 정보서비스_GW(지역별 방문자수)` 활용신청이 승인**되어, 같은 인증키로 `DataLabService/locgoRegnVisitrDDList` 가 `resultCode 0000` 응답.
- **API 계약 차이 발견·수정:** 이 _GW 서비스는 `signguCd`/`areaCd` 필터 파라미터를 **받지 않고**(전달 시 `INVALID_REQUEST_PARAMETER_ERROR`) 전국 시군구를 한 번에 반환 → **클라이언트에서 경북(47xxx)만 필터·집계**하도록 `fetchGyeongbukVisitors` 전면 재작성.
- **지표 정의:** 인구비례로 쏠리는 `현지인(touDivCd=1)` 은 제외하고 **외지인(2)+외국인(3)** 만 합산, 최근 가용 월의 대표 주간(8~14일) 누계.
- **실데이터 렌더 검증(2026.04 기준):** /insights 방문자 랭킹 상위값 `1,114,668 / 1,018,168 / 542,341 / 490,286 …` 정상 표시(콤마·단위 포함), activate 카드로 빠지지 않음.

### #4(이전 🟠) ESLint 게이트 실패(20 errors) → **0 errors**
- `react-hooks/refs` ×3(useApi·IllustratedMap): 렌더 중 ref 쓰기 → 커밋 후 effect 동기화.
- `react-hooks/set-state-in-effect` ×12: fetch effect는 async 래핑, 단순 리셋은 렌더 중 파생, SmartHints는 useMemo로 전환.
- `no-unused-vars`(tour.ts) / `no-explicit-any`(KakaoMap) / `use-memo`(AddToHomeDialog) / 스테일 disable 정리.
- 회귀 없음: 변경된 6개 핵심 화면 헤드리스 렌더 정상.

---

## ❌ 안 되는 부분 / 남은 이슈

### 🟠 #2 빅데이터 "연관추천(TarRlteService1)" 아직 미신청
- `TarRlteService1/keywordBasedList1` 은 여전히 평문 **"Unexpected errors"**(미신청) 반환.
- 영향: PlaceDetail "함께 찾은 곳"은 자동 숨김, /insights "빅데이터가 주목한 관광지"는 *"활용신청 후 자동 표시됩니다"* 안내 노출 — **graceful 동작 정상**.
- 조치: 공공데이터포털에서 *"한국관광공사_관광지 연관 추천 정보 서비스"* 1건 추가 활용신청(무료). 같은 키로 자동 점등.

### 🟠 #3 로컬 `npm run build` 크래시 — 환경 이슈(변동 없음)
- 한글 경로(`D:\심소현\...`)에서 `vite build` 가 `STATUS_STACK_BUFFER_OVERRUN` 으로 죽음. ASCII 경로 복사 시 정상. Vercel(Linux) 배포 무관, 로컬 프로덕션 빌드 검증만 제약.

### 🟡 #5 단일 번들 ~684KB
- 코드 스플리팅 미적용. 차단 아님. `React.lazy` 라우트 분할 권장(미적용).

### 🟡 #6 og-image 추출 폴백
- 일부 사이트 미리보기 이미지 누락 가능. 폴백 정상, 차단 아님.

---

## 🔧 신규 반영 — 포트 5173 고정(요청사항)

### #4(신규) 쉼마루가 항상 5173 을 점유하도록 수정 → **완료·검증**
- 카카오 지도/공유 도메인이 `localhost:5173` 에만 등록돼 있어 strictPort 5173 필수인데, 타 프로젝트(예: "ITAM 기준정보관리")가 5173 을 점유하면 vite 가 기동 실패.
- **조치:** `frontend/scripts/free-port.cjs` 추가 + `package.json` 에 **`predev`** 등록 → `npm run dev` 직전 5173 점유 프로세스를 자동 종료(5173 확보).
  - win32: PowerShell `Get-NetTCPConnection` 우선(IPv6 `[::1]:5173` 포함), netstat 폴백. (※ `netstat -p tcp` 는 IPv6 누락 → 필터 없이 매칭하도록 처리)
- **검증:** predev 가 5173 점유 PID 31052(node.exe) 종료 후 vite 가 5173 기동, `http://localhost:5173/` 타이틀이 "쉼(休)마루"로 확인.

---

## 📌 검사 못 한 부분(한계)
- **카카오맵 실제 렌더링·공유:** 헤드리스 DOM 검증만 수행, 지도 타일/카카오 공유 시트 육안 검증은 미수행. (이제 5173 점유 가능하므로 브라우저로 직접 확인 권장)
- **프로덕션 빌드 산출물:** 한글경로 크래시(#3)로 로컬 build 미수행. dev/tsc/lint 통과로 갈음.

---

## 종합 판정
이전 보고서의 핵심 결함 **#1(빅데이터 분류 버그)과 #4(ESLint 20 errors)는 모두 해소**되었고, **데이터랩 방문자수 빅데이터가 실데이터로 정상 연동**(데이터 활용도 심사 핵심)됨을 확인. 요청한 **포트 5173 고정 점유**도 predev 스크립트로 구현·검증 완료. 남은 항목은 연관추천 활용신청(#2, 팀 액션·무료)과 환경/성능성 비차단 이슈(#3·#5·#6)뿐이다.

**우선순위 권고:** #2(연관추천 활용신청) → #5(번들 코드 스플리팅, 선택).
