# 쉼(休)마루 품질테스트 보고서

- **검증일시**: 2026-06-12 10:43
- **검증자**: QA (심소현)
- **검증대상**: dev 서버(localhost:5173) + 운영(shimmaru.vercel.app) + 전체 소스
- **검증범위**: ① 정적 게이트(tsc·eslint·프로덕션 빌드) ② OpenAPI 실호출 8종 ③ 전회(6/5) 지적사항 전수 재검증 ④ 신규 코드 정밀 리뷰(챗봇·코스엔진·i18n·UI/UX) ⑤ 공모전 필수 제약 준수
- **검증방법**: dev 프록시 실호출 + 운영 URL 실호출 + 정밀 코드 리뷰 4트랙 병렬(챗봇 / 코스엔진·API / i18n·하드코딩 / 페이지 UI·UX·접근성) + 빌드 검증

---

## 1. 종합 요약

| 구분 | 결과 |
|---|---|
| 정적 게이트 | tsc **0 에러** · eslint **0 에러**(경고 4) · 프로덕션 빌드 **성공**(PWA precache 31개) |
| API 실작동 | 8종 중 **7종 정상**. 전회 미구독이던 **연관추천(TarRlte) 정상화**, 전회 미연동이던 **날씨 실연동 완료**. 반려동물 API만 미신청(404, graceful 폴백 확인) |
| 전회 지적 재검증 | 높음 3/3 수정, i18n 8건 중 6건 수정, UI/UX 13건 중 11건 수정 — **24건 중 20건 해소** |
| 신규 발견 | **높음 4건**(공모전 금지어 노출 포함), 중간 12건, 낮음 다수 |
| 운영 배포 | shimmaru.vercel.app HTTP 200, 운영 프록시 경유 TourAPI 실응답(resultCode 0000) 확인 |

> **핵심 판정**: API 실연동·빌드·배포 등 공모전 골격은 전회 대비 크게 강화됐다(빅데이터 2종 + 날씨 실연동 추가). 다만 **"한국관광공사/KTO" 명칭이 사용자 화면에 노출**되는 건은 공모전 필수 제약("서비스 내 한국관광공사·KTO 단어/로고 사용 금지") **직접 위반**이므로 출품 전 반드시 제거해야 한다.

---

## 2. 정적 게이트

| 게이트 | 결과 | 비고 |
|---|---|---|
| `tsc -b` | ✅ 0 에러 | |
| `eslint .` | ✅ 0 에러 / 경고 4 | react-hooks/exhaustive-deps 4건(전회와 동일, KakaoMap·Explore·FestivalDetail·PlaceDetail) |
| `npm run build` | ✅ 성공 | ASCII 경로 사본 기준. PWA precache 31개(758KB), sw.js 정상 생성. 메인 청크 558KB(gzip 188KB) — 경고 수준 |
| 한글 경로 빌드 | ⚠️ 크래시 재현 | 알려진 환경 이슈(exit 9, 청크 렌더링 중 프로세스 사망). **코드 문제 아님** — Vercel(리눅스)은 정상 빌드·배포됨. 정션(junction) 우회는 realpath 불일치로 불가, 소스 복사로만 우회 가능 |

---

## 3. API 작동 검증 (실호출 결과)

dev 프록시(`/api/*`) 실호출 기준. 운영(Vercel) 프록시도 동일 구조 확인.

| # | API | 서비스/엔드포인트 | 결과 | 비고 |
|---|---|---|---|---|
| 1 | 관광정보 | TourAPI `KorService2/areaBasedList2` | ✅ 정상 | resultCode 0000, 경북 실데이터 |
| 2 | 표준축제 | 행안부 `tn_pubr_public_cltur_fstvl_api` | ✅ 정상 | NORMAL_SERVICE, 2026년 행사 |
| 3 | 방문자통계 | DataLab `locgoRegnVisitrDDList` | ✅ 정상 | resultCode 0000 |
| 4 | **연관추천** | `TarRlteTarService1/areaBasedList1` | ✅ **정상 (전회 미구독 → 활용신청 승인 완료)** | resultCode 0000, 안동시 연관 관광지 실데이터 — Insights·PlaceDetail 연관추천 활성화됨 |
| 5 | **날씨** | 기상청 `VilageFcstInfoService_2.0/getVilageFcst` | ✅ **정상 (전회 미연동 → 실연동 완료)** | NORMAL_SERVICE, POP 실예보 사용 + 예보범위 밖·실패 시 평년값 폴백 구조 견고(`weather.ts:76-119`) |
| 6 | 수상사진 | `PhokoAwrdService/phokoAwrdSyncList` | ✅ 정상 | resultCode 0000 |
| 7 | 템플스테이 | templestay.com HTML 프록시 | ✅ 정상 | HTTP 200, 63KB HTML |
| 8 | 반려동물 | `KorPetTourService/areaBasedList` | ❌ **404 (활용신청 미승인 추정)** | 잘못된 경로도 동일하게 404를 반환하므로(베이스라인 테스트) 게이트웨이가 미신청 서비스 경로를 미라우팅하는 것으로 판단. **폴백 검증 완료**: 404 → 빈 결과 → 일반 검색 폴백(`Home.tsx:181-184`), 가짜 데이터·임의 pet 태깅 없음 |
| + | og-image | dev 미들웨어 + Vercel Edge | ✅ 정상 | SSRF 가드(사설 IP 차단) 확인 |
| + | 운영 URL | shimmaru.vercel.app | ✅ 정상 | 본체 200 + 운영 프록시 경유 TourAPI 0000 |

**운영 프록시 구성 검증**: `vercel.json` rewrites + `api/{tour,weather,festival-std,templestay,og-image}.ts` 5개 Edge 함수가 dev 프록시와 동일 인터페이스. `api/tour.ts`는 B551011 전 서비스(빅데이터·수상사진·반려동물 포함) 범용 포워딩이라 활용신청만 승인되면 운영도 즉시 활성화.

---

## 4. 전회(6/5) 지적사항 재검증

### 높음 3건 — 전부 수정 ✅
| 항목 | 판정 | 근거 |
|---|---|---|
| 딥링크 축제 상세 날짜 누락 | **FIXED** | `tour.ts:759-805` — std는 캐시 목록에서 전체 복원, TourAPI는 detailCommon2로 기간 복원 |
| 에러화면 `error.goHome` 키 깨짐 | **FIXED** | `RouteError.tsx:40` — `t('common.goHome')` |
| `/admin` 프로덕션 노출 | **FIXED** | `routes.tsx:46` — DEV에서만 라우트 등록, 푸터 링크도 DEV 가드 |

### 중간·낮음 21건 — 17건 수정
**FIXED**: Festivals 지도/캘린더 로딩·에러 처리, Home 쇼케이스 에러 처리, GlobalSearch 데드코드(파일 삭제), CourseMap/Edit 렌더 중 nav 호출, CourseEdit 재계산 기준점(baseCenter 코스에 저장), **수상사진 촬영자명 ⓒ 표기(저작권 요건)**, Festivals 카드 키보드 접근, CourseMap 상세 버튼 분리, Journal 별점 aria, decodeShare 최소 검증, KakaoMap CATEGORY_MAP 폴백, kakaoShare '자세히 보기' i18n화, CourseResult share/단위, CourseMap close, Home n박n일, Favorites remove, Explore 테마해제 aria

**STILL PRESENT** (4건):
| 심각도 | 내용 | 위치 |
|---|---|---|
| 높음→재상정 | Favorites 찜 기반 코스 생성 실패 시 무음(catch 없음 + `void` 호출 → unhandled rejection) | `Favorites.tsx:43-74,148` |
| 중간 | Settings 버전 `v2.1` 수기 리터럴(package.json은 0.0.0 — 출처 없음) | `Settings.tsx:92` |
| 중간 | Admin 랭킹에 원시 식별자(hanok, ko) 그대로 노출 | `Admin.tsx:48,61` |
| 낮음 | PlaceDetail 라이트박스 — 초기 포커스·복원은 추가됐으나 Tab 트랩은 여전히 없음 | `PlaceDetail.tsx:111-117,322-382` |

---

## 5. 신규 발견 결함

### 높음 (출품 전 필수 수정)

| # | 내용 | 위치 |
|---|---|---|
| 1 | **공모전 금지어 "한국관광공사/KTO" 사용자 화면 노출** — OT자료 필수 제약("서비스 내 한국관광공사·KTO 단어/로고 사용 금지") 직접 위반. 4개 로케일 사용자 노출 문자열 약 20곳: Explore 무장애 안내(`explore.a11yHint/a11ySource/a11yForbiddenTitle/Body`), Insights 부제(`bigdata.insightsSubtitle/activateBody/notSubscribed`), 푸터 출처(`footer.apiSource` = "한국관광공사 OpenAPI"). en은 "KTO", ja "韓国観光公社", zh "韩国旅游公社"로 번역까지 됨. "공공데이터포털 OpenAPI", "관광 빅데이터" 등 중립 표현으로 일괄 치환 필요 (코드 주석은 무관) | `ko.ts:283,288,290,581,586,597,607` + en/ja/zh 동일 위치, 렌더처 `Explore.tsx:368,466,472-474` · `Insights.tsx:48` · `RelatedSpots.tsx:82` · `AppShell.tsx:194` |
| 2 | **축제 상세 "캘린더 추가" 버튼이 4개 언어 전부 raw 키 노출** — `t('festivals.addToCalendar')` 호출하지만 키는 `place.addToCalendar`에만 정의됨 → 화면에 "＋ festivals.addToCalendar" 그대로 표시. 심사위원이 축제 상세만 열어도 보이는 결함 | `FestivalDetail.tsx:150` (수정: `t('place.addToCalendar')`) |
| 3 | **좌표 없는 표준데이터 축제가 (0,0)으로 코스 진입 → 총거리 ~8,900km 폭발 + 지도 폴리라인 이탈** — `mapStdToFestival`이 위경도 누락 시 `{lat:0,lng:0}` 생성, `generateCourse`는 좌표 검증 없이 push. 축제연계 코스에서 실제 도달 가능한 경로 | `standardFestival.ts:157-158`, `courseEngine.ts:230-233` (수정: 좌표 0 가드 한 줄) |
| 4 | **Favorites 코스 생성 무음 실패** (전회 잔존 — 재상정) — try/finally에 catch 없음. API 실패 시 버튼만 원복되고 아무 안내 없음 | `Favorites.tsx:43-74,148` |

### 중간

| # | 내용 | 위치 |
|---|---|---|
| 1 | 챗봇: 언어 전환 시 기존 말풍선이 이전 언어로 잔존 → 한·영 혼합 트랜스크립트(다국어 어필 데모에서 노출) — `buildLog(step)` 재구성으로 해결 가능 | `TripChatbot.tsx:91,106-114,186-196` |
| 2 | 챗봇/빌더: 날짜 input을 비우면 **"NaN박 NaN일"** 표시 + 빈 날짜로 확정 가능(Invalid Date가 날씨 조회·축제 필터로 전파) | `TripChatbot.tsx:496-526` + `Home.tsx:449-470` |
| 3 | 코스 생성 실패 시 원인 불문 "인식한 키워드가 없어요" 토스트 — 챗봇은 버튼식이라 API 장애 시 엉뚱한 안내 | `Home.tsx:238-240` |
| 4 | 종료된 축제가 코스에 포함 가능 — `festivalOverlaps` ±7일 여유가 코스 엔진에도 적용(여행 시작 7일 전 끝난 축제 매칭). 목록용 여유와 달리 일정 편입은 실겹침만 허용해야 함 | `courseEngine.ts:354-361` |
| 5 | `inferCategory`에 trail/templestay 분기 부재 → 코스 생성 경로에서 해당 카테고리가 생성되지 않아 **반려동물=둘레길 우선 가중치·quota가 사실상 무효**(pet 차별화 로직 dead code) | `tour.ts:1034-1046`, `courseEngine.ts:61,467-472` |
| 6 | 축제 슬롯 예약(-1)이 top-up 루프에 의해 무효 → festival_link 코스가 목표+1개 장소(당일치기 4→5곳) | `courseEngine.ts:216-233` vs `454-458` |
| 7 | Festivals 카드 찜 토글이 raw `<button>` — aria-label 없음(공용 FavoriteStar 미사용) | `Festivals.tsx:160-174` |
| 8 | 라이트박스·홈 빌더 모달 Tab 포커스 트랩 부재(빌더 모달은 초기 포커스 이동도 없음) | `PlaceDetail.tsx:322-382`, `Home.tsx:363-556` |
| 9 | aria-label 하드코딩: 줌 컨트롤 "줌인/줌아웃/리셋"(한국어 — 외국어 스크린리더에 한국어 발화), `menu`, `notifications`/`close`, `drag` | `IllustratedMap.tsx:337,345,353`, `AppShell.tsx:92`, `ToastHost.tsx:26,61`, `CourseEdit.tsx:158` |
| 10 | 지도 인셋 라벨 울릉·독도 SVG 한글 하드코딩(독도 한글 표기는 정책 의도일 수 있음 — 병기 검토) | `UlleungInset.tsx:40,47,72` |
| 11 | Settings v2.1 수기 버전 / Admin 원시 식별자 (전회 잔존, §4 참조) | `Settings.tsx:92`, `Admin.tsx:48,61` |
| 12 | 평문 응답을 무조건 미구독(FORBIDDEN)으로 분류 — 게이트웨이 점검 등 일시 오류도 "활용신청 누락"으로 콘솔 안내(폴백 동작은 동일) | `tour.ts:178-181,204` |

### 낮음 (요약)

- 챗봇: 타이핑(480ms) 중 빠른 클릭 시 직전 봇 질문 버블 유실 / 메시지 영역 `aria-live` 없음 / 4번째 지역 선택 시 첫 칩 무경고 해제 / 과거 날짜 선택 차단 없음 / ✓·📅 장식문자 aria 처리 누락 (`TripChatbot.tsx`)
- 코스엔진: `matchingFestivals[0]` 임의 선택(거리·점수 미반영) / 2-opt가 시군구 클러스터 묶음을 되섞을 수 있음(실해 작음) / `distanceFromPrevKm` 주석-구현 불일치 / accessible+pet 동시 선택 시 dedup이 pet 태그 유실 / pet 미구독 시 음성 캐시 없어 매번 재호출 / `'사'` 한 글자 포함 휴리스틱 오분류 가능 (`courseEngine.ts`, `tour.ts`, `Home.tsx:186-192`)
- i18n: Explore 반경 칩 `km` 단위 하드코딩(zh는 '公里' 번역 보유) / 코스 제목 폴백 ja·zh에 영문 "Gyeongbuk" / `parseTripIntent.ts` 죽은 모듈 / 구 UI 잔존 키(`home.heroTitle1~3`, `home.nl*`, `categoryDesc` 10키 등) / en `visitorsUnit` 빈값(미참조 키)
- 접근성: Explore 페이지네이션 ←/→ aria-label 없음 / decodeShare 내부 필드(position 등) 미검증
- eslint 경고 4건(exhaustive-deps) 정리 검토

**i18n 키 패리티**: 4개 로케일 **534키 완전 일치**(누락 없음), 사용 키 287개 전수 대조에서 미정의 키는 높음 #2 한 건뿐.

---

## 6. 우선 조치 권고 (출품 전)

### 즉시 수정 (높음)
1. **금지어 제거** — `한국관광공사/KTO/韓国観光公社/韩国旅游公社`를 4개 로케일 사용자 문자열에서 일괄 치환(예: "공공데이터포털 OpenAPI", "관광 빅데이터"). 푸터 `footer.apiSource` 포함.
2. `FestivalDetail.tsx:150` — `t('festivals.addToCalendar')` → `t('place.addToCalendar')` (한 줄).
3. `courseEngine.ts:230-233` — 축제 push 전 `fest.position.lat && fest.position.lng` 가드 (한 줄).
4. `Favorites.tsx` — buildFromFavorites에 catch + 에러 토스트.

### 권장 (중간, 데모 품질)
5. 챗봇 언어 전환 시 `setMsgs(buildLog(step))` 재구성 + 빈 날짜 NaN 가드(confirmDates disabled).
6. 코스 엔진 축제 겹침 여유 0일로(종료된 축제 편입 차단) + festival_link 슬롯 초과 수정.
7. `inferCategory`에 trail/templestay 분기 추가 — 반려동물 차별화 로직 활성화.
8. **KorPetTourService 공공데이터포털 활용신청** — 승인 시 코드 수정 없이 즉시 활성화(운영 프록시도 커버 확인됨).
9. 생성 실패 토스트 문구 분리(키워드 미인식 vs API 오류).

### 검토 (낮음)
- aria-label i18n화(줌·menu·drag·notifications), 포커스 트랩, 죽은 모듈·잔존 키 정리, Settings 버전 일원화.

---

## 7. 공모전 요건 대비 종합

| 요건 | 상태 |
|---|---|
| OpenAPI 실연동(파일데이터 불인정) | ✅ **충족·강화** — TourAPI·표준축제·데이터랩·연관추천·수상사진·날씨·템플스테이 7종 실연동, mock 완전 제거, 활용신청 기반 호출 로그 축적 중 |
| 빅데이터 활용 | ✅ 연관추천(TarRlte) **승인 완료로 실데이터 활성화** + 데이터랩 방문자통계 — Insights 화면·코스 점수 반영 |
| 서비스 URL 제출(웹) | ✅ shimmaru.vercel.app 가동, PWA 설치 가능(precache 31개) |
| 한국관광공사·KTO 명칭 금지 | ❌ **위반 — 높음 #1, 출품 전 필수 제거** |
| 사진 저작권 표기 | ✅ 수상사진 ⓒ촬영자명 + 출처 문구(전회 지적 수정 확인) |
| 지역특화(+2점) | ✅ 경북 특화 유지 |
| 안정 구동 완제품 | ⚠️ 양호하나 높음 #2~4(raw 키 노출·코스 거리 폭발·무음 실패)는 심사 중 마주칠 수 있는 결함 — 수정 권장 |

> 본 보고서는 코드 정독 + dev/운영 API 실호출 + 프로덕션 빌드 검증 기준. 실기기 브라우저 시각 검증(반응형·픽셀)은 별도 수동 테스트 권장.
