# Shimmaru(쉼마루) 배포 준비 개선 작업 보고서

- **작업일시:** 2026-06-05 11:03
- **작업자:** 심소현
- **목표:** 무료 배포 가능 수준 — ① 불편함 제로 ② 누구나 쉽게 ③ 다시 찾게
- **기준:** 전면 UX 감사(22항목) + 품질테스트_20260604_0949.md 후속

---

## ✅ 이번에 적용·검증 완료한 개선 (코드)

| # | 항목 | 파일 | 효과(원칙) |
|---|---|---|---|
| 1 | 축제 목록 부분실패 허용 + 페이지별 2회 재시도 | `api/standardFestival.ts` | 업스트림 간헐 500에도 목록이 통째로 비지 않음 (①) |
| 2 | 코스 생성 `Promise.all`→`allSettled` | `pages/Home.tsx` | 시군 한 곳 실패가 생성 전체를 막지 않음·무한 스피너 제거 (①) |
| 3 | 찜 기반 생성 `Promise.all`→`allSettled` | `pages/Favorites.tsx` | 동일 (①) |
| 4 | 라우트 코드 스플리팅(`React.lazy`)+`Suspense` 폴백 | `routes.tsx`, `AppShell.tsx` | 초기 번들 축소·첫 로딩 단축 (①) |
| 5 | 라우트 에러 경계(`RouteError`) | `components/RouteError.tsx`, `routes.tsx` | 렌더/청크 로드 실패 시 백지 대신 복구 화면(새로고침·홈) (①) |
| 6 | PWA 네이티브 설치 프롬프트 캡처 | `lib/pwaInstall.ts`, `main.tsx` | `beforeinstallprompt` 보관→원탭 설치 (②③) |
| 7 | 설치 다이얼로그 원탭 설치 연동 | `components/AddToHomeDialog.tsx` | Chromium은 즉시 설치, iOS는 수동 폴백 (②③) |
| 8 | 안드로이드 호환 PNG 아이콘 192/512 생성·등록 | `public/icon-192.png`·`icon-512.png`, `vite.config.ts`, `public/manifest.webmanifest` | SVG 단독 설치 실패 기기 대응 (②③) |
| 9 | RouteError/설치 i18n 키 4개 언어 추가 | `i18n/locales/{ko,en,ja,zh}.ts` | 다국어 정합 |

### 검증 결과
- `tsc -b`: 에러 0
- `eslint .`: **0 errors** / 4 warnings(기존 exhaustive-deps, 비차단)
- dev(5173) HMR·config 재시작 에러 0, 변경 모듈 전부 트랜스폼 200
- 신규 아이콘·manifest 서빙 200, 512 아이콘 육안 확인(크림 배경+안전영역, 마스커블 적합)

---

## 🔴 남은 배포 차단 요인 — 팀(콘솔/포털) 작업 필요

1. **카카오 운영 도메인 등록 확인** — `shimmaru.vercel.app`이 카카오 콘솔(플랫폼 Web + 카카오맵 + 공유 링크관리)에 등록돼야 운영에서 지도·공유 동작. 미등록 시 라이브에서 지도 안 뜸. **육안 확인 필수.**
2. **공유 serviceKey 호출 한도** — 전 사용자가 키 1개 공유. 개발등급(~1000/일)이면 트래픽 시 데이터 빈 화면. 운영등급 한도 확인.
3. **연관추천(TarRlteService1) 무료 활용신청** — 미신청 상태(graceful 숨김). 신청 시 "함께 찾은 곳"·insights 자동 점등.

---

## 🟠🟡 남은 UX 개선(감사 22항목 중 미적용) — 우선순위

- 🟠 첫 실행 온보딩 뒤로가기 버튼, 무장애 토글 시각적 강조, 언어 선택 체크표시(색상 외 신호)
- 🟠 재방문 훅: Favorites "이어서 보기"(최근 코스), 저장일 표시, 축제 캘린더 추가(.ics)
- 🟡 공유 링크 OG 프리뷰, og-image 폴백, exhaustive-deps 경고 4건 점검

---

## 📌 한계(미검증)
- 카카오맵 실제 타일 렌더·공유 시트, 네이티브 설치 프롬프트 실제 팝업은 브라우저 자동화 도구 부재로 육안 미검증(헤드리스 DOM/트랜스폼까지만). 운영 도메인에서 직접 확인 권장.
- 로컬 `vite build`는 한글경로 크래시 지속(환경 이슈, Vercel 빌드 무관) — 코드 스플리팅 청크 산출물은 Vercel/ASCII 경로 빌드에서 확인 필요.
