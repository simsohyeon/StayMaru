# Shimmaru(쉼마루) 데이터 활용도·재방문 개선 작업 보고서

- **작업일시:** 2026-06-05 12:57
- **작업자:** 심소현
- **목표:** "활용은 하나 활용도 낮은 데이터" 끌어올리기 + 추가/삭제 정리 (사용자 불편 제로·누구나 쉽게·다시 찾게)
- **선행:** 품질테스트_20260605_1103.md (안정성·코드스플리팅·PWA설치·아이콘)

---

## ✅ 이번 라운드 완료 (5건, 전부 tsc·lint·트랜스폼 검증)

### 1. DataLab 방문자 빅데이터 → 코스엔진 숨은지역 점수 연결 🔴
- 기존: 숨은지역 점수가 정적 `hiddenBoost`(상수). DataLab은 `/insights` 표시용으로만 소비.
- 변경: `lib/visitorIndex.ts` 신설 — `fetchGyeongbukVisitors()` 실데이터를 로그 정규화해 시군별 한적함 boost(0~1) 생성. `courseEngine.scoreOf`·`slowIndex`가 **DataLab boost 우선, 미구독 시 정적 폴백**.
- 가시화: 코스 결과 Slow Index 카드에 "한적 지수에 데이터랩 실방문자({{ym}}) 반영" 출처 라벨.
- 의의: "숨은 시군을 데이터로 끌어올린다"는 정체성을 정적표가 아닌 **실데이터로 구현** → 공모전 데이터 조합·재가공 점수 직결.

### 2. 기상청 강수확률 정직화 🔴(신뢰 리스크 제거)
- 기존: `weather.ts`가 평년값 하드코딩인데 UI/README는 "강수확률(기상청)" — 라이브 예보처럼 오인.
- 변경: SmartHints·빌더·README 표기를 **"평년 강수 경향(기상청 평년값 근사)"**으로 4개 언어 정정. README에 "실시간 단기예보는 기상청 OpenAPI 키 확보 시 활성화(격자좌표 준비됨)" 명시.
- 의의: OT FAQ의 "파일·하드코딩 데이터 불인정"과의 표기 불일치 리스크 제거.

### 3. 사진 수상작 갤러리 추가 (PhokoAwrdService) ➕
- `api/photoAwards.ts` 신설 — 기존 `/api/tour` 프록시 재사용(앱 키로 resultCode 0000 확인). 전국 96건 중 **경북(47) 필터** + 촬영지→시군 매핑.
- Insights에 "경북 수상 사진" 섹션(데이터 있을 때만 노출) + 촬영자·출처 표기(저작권 Type1). 공사 OpenAPI 1종 추가 활용.

### 4. 하단 탭 재배치 + 푸터 데드링크 정리 ➖
- 하단 5탭: `Journal`(개인일지) → **`Insights`(데이터 쇼케이스)** 로 교체. Journal은 햄버거 메뉴로 강등(접근 유지).
- 푸터 `/explore?cat=festival`(폐기된 리다이렉트) → `/festivals` 교체.

### 5. 재방문 훅 ➕
- Favorites 상단 **"이어서 보기"** 카드 — `courses.recent[0]` 재개(원탭 코스 복귀).
- FestivalDetail **"캘린더에 추가"(.ics)** — `lib/ics.ts` 표준 VEVENT 생성·다운로드(외부 의존성 0). 진행/예정 축제만 노출.

---

## 🔢 검증
- `tsc -b` 0 errors · `eslint` 0 errors/4 warnings(기존) · 변경 모듈 전부 트랜스폼 200
- i18n: 신규 키 10종 ko/en/ja/zh **4/4 전수 존재**. dev(5173) HMR·root 200 정상.

## 🔴 팀 액션 (코드 외)
- **연관추천(TarRlteService1) 무료 활용신청** — 하면 "함께 찾은 곳" 자동 점등. (DataLab·사진수상작은 이미 앱 키로 동작 확인)
- 카카오 운영 도메인(shimmaru.vercel.app) 콘솔 등록 확인(이전 보고서 #1).

## 🟡 남은 UX 개선(미적용, Explore 감사 잔여)
- 온보딩 뒤로가기, 언어선택 체크표시(색맹 대응), 무장애 토글 강조, 공유 링크 OG 프리뷰, 코스 저장일 표시.

## 📌 한계
- 카카오맵 실타일/설치 팝업/실제 .ics 임포트 육안 미검증(브라우저 자동화 부재). 운영 도메인 직접 확인 권장.
